Sparkle feeds. Written by the release workflow with Sparkle's `generate_appcast`,
signed with the EdDSA key whose public half is `SUPublicEDKey` in the app.

Do not hand-edit: a manual change invalidates the signature for that item.
