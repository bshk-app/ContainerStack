<?xml version="1.0" standalone="yes"?>
<rss xmlns:sparkle="http://www.andymatuschak.org/xml-namespaces/sparkle" version="2.0">
    <channel>
        <title>ContainerStack</title>
        <item>
            <title>0.4.1</title>
            <pubDate>Tue, 25 Aug 2026 23:42:28 +0300</pubDate>
            <sparkle:version>151</sparkle:version>
            <sparkle:shortVersionString>0.4.1</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>26.0</sparkle:minimumSystemVersion>
            <sparkle:hardwareRequirements>arm64</sparkle:hardwareRequirements>
            <description><![CDATA[<p>Starting and stopping containers works again on a machine where another Docker<br>
bridge got there first, and one slow container no longer freezes the rest.</p>
<h3>Fixed</h3>
<ul>
<li>Another program listening on the Docker socket is now named instead of adopted.<br>
A bridge from a different build answers every status check while start and stop<br>
hang - measured past 150 seconds against a container the bundled bridge started<br>
in 2 - so the app reported a healthy engine while nothing could be controlled.<br>
It now says which socket is held and what to do, keeps saying it while it is<br>
true, and stops offering the actions that cannot succeed. Listing containers and<br>
reading their logs keep working, since that is what you need at that point.</li>
<li>Acting on one container no longer disables every other one. Stopping can take<br>
the full two-minute timeout, and for that whole time no other container could be<br>
started, stopped, deleted or have its logs opened - and a click on a different<br>
container did nothing at all, without a word.</li>
<li>Quitting no longer leaves runtime commands running. A command the app was<br>
waiting on used to survive the app that started it: eight of them were found on<br>
one machine, the oldest four days old, each wedged and invisible. The Docker<br>
bridge is unaffected and still outlives the app on purpose.</li>
</ul>]]></description>
            <enclosure url="https://dl.bshk.app/products/containerstack/releases/0.4.1/59686655-4e4e-420f-8c7c-48904ad6177f/ContainerStack-0.4.1.dmg" length="133483230" type="application/octet-stream" sparkle:edSignature="v0vgbKWRBjf5HtAnymr9Jknk+kgHAhEN/lWxWG5w/SYFRB/GOvUIQAilfwswM7x7Ug1SornF4Z1WFXAXSuOLDQ=="/>
        </item>
        <item>
            <title>0.4.0</title>
            <pubDate>Mon, 24 Aug 2026 18:14:10 +0000</pubDate>
            <sparkle:version>140</sparkle:version>
            <sparkle:shortVersionString>0.4.0</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>26.0</sparkle:minimumSystemVersion>
            <sparkle:hardwareRequirements>arm64</sparkle:hardwareRequirements>
            <description><![CDATA[<p>The app can finally update itself. Beyond that: runtime control is harder to<br>
wedge, destructive actions are harder to trigger by accident, and diagnostics<br>
are more honest.</p>
<h3>Added</h3>
<ul>
<li>In-app updates. Every build since 0.1.0 advertised an update feed and the<br>
signed feed has been live the whole time, but nothing read it: there was no<br>
Sparkle, no menu item, and no way to learn a new version existed. "Check for<br>
Updates…" now sits in the app menu and the menu bar. Checking happens on its<br>
own; installing waits for you, because replacing the app while containers run<br>
is not a decision to make on your behalf.</li>
<li>Docker sections can be hidden from the sidebar through its context menu. Even<br>
with every Docker item hidden, the same menu remains available to restore them.</li>
<li><code class="notranslate">cstack doctor</code> reports the configured memory limits of running containers<br>
against host memory. Unlimited containers and failed inspections are named<br>
separately instead of being counted as zero.</li>
<li>Every icon-only action has a descriptive, resource-specific VoiceOver name.</li>
</ul>
<h3>Fixed</h3>
<ul>
<li>Releases carry their download again. 0.3.0 was published before its DMG was<br>
built, and GitHub freezes a release's files the moment it is published, so<br>
that version shipped empty and could not be repaired. The installer is now<br>
attached while the release is still a draft, and publishing is the last step.</li>
<li>Every subprocess wait now has an explicit deadline, except the deliberately<br>
supervised socktainer process. A child that hangs — or exits while a descendant<br>
keeps its output pipe open — can no longer wedge monitoring and runtime controls<br>
forever.</li>
<li><code class="notranslate">cstack runtime restart</code> continues best-effort recovery after a failed step,<br>
names each failure, and exits non-zero instead of claiming success.</li>
<li>GUI, helper, <code class="notranslate">cstack runtime</code>, and <code class="notranslate">cstack doctor</code> resolve the Apple Container<br>
binary and its install root through one owner. Environment overrides and the<br>
vendored runtime no longer apply to only part of the system.</li>
<li>The bridge no longer writes an INFO request line for every 3-second poll into<br>
<code class="notranslate">runtime.log</code>; warnings, errors, startup and DNS fallback messages remain.</li>
<li>Deleting a container, image, volume, or network now requires confirmation.<br>
In-use volumes are not force-deleted.</li>
<li>Malformed chunked HTTP sizes return a parse error instead of trapping on<br>
integer overflow or a reversed range.</li>
<li>Image creation time reports unknown when the runtime returns zero, rather than<br>
“56 years ago”. Architecture and OS now come from image inspect, the endpoint<br>
that actually provides them.</li>
</ul>
<h3>Changed</h3>
<ul>
<li>The expensive missing-app-root diagnostic runs at most every 30 seconds<br>
instead of spawning the <code class="notranslate">container</code> CLI on every 3-second monitor tick. Manual<br>
refresh still probes immediately.</li>
</ul>
<h3>Known limitations</h3>
<p>Published ports still require a runtime restart if a bridge-created network's<br>
vmnet helper dies; restarting only the containers does not repair the host route.<br>
The per-container micro-VM architecture also remains slower than OrbStack on the<br>
measured M1 system (4.6x container round trip, 3.6x bind-mount writes).</p>]]></description>
            <enclosure url="https://github.com/bshk-app/ContainerStack/releases/download/v0.4.0/ContainerStack-0.4.0.dmg" length="22415085" type="application/octet-stream" sparkle:edSignature="ih5JMvLf/p66bzE6RQAZgP+irE76kQDLxwBQJhms357caHsfgs+SVgR9OKrJxV4WyTNyQ/apEO3IYzmJ+AcoAw=="/>
        </item>
        <item>
            <title>0.2.0</title>
            <pubDate>Sun, 23 Aug 2026 20:00:23 +0000</pubDate>
            <sparkle:version>83</sparkle:version>
            <sparkle:shortVersionString>0.2.0</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>26.0</sparkle:minimumSystemVersion>
            <sparkle:hardwareRequirements>arm64</sparkle:hardwareRequirements>
            <description><![CDATA[<p>Updates now come from GitHub. Nothing in the app itself changed: no file under<br>
<code class="notranslate">Sources/</code> differs from 0.1.0, and the only edit to the shipped bundle is the<br>
update feed it points at.</p>
<h3>Changed</h3>
<ul>
<li>Updates are served from a signed feed on GitHub Pages, and the DMG is a GitHub<br>
Release asset. Both used to come from a server that had to be reachable for an<br>
update to be found at all; now the artifact lives where the source does.</li>
<li>The update signing key was rotated as part of the move, so this build trusts a<br>
different key than 0.1.0 did.</li>
</ul>
<h3>Known limitations</h3>
<p>Unchanged from 0.1.0, and still the two worth knowing before installing:<br>
published ports stop working until the runtime is restarted when a<br>
bridge-created network's vmnet helper dies, and ContainerStack is slower than<br>
OrbStack on an idle M1/16 GiB — container round trip 4.6x, bind mount writes<br>
3.6x, because Apple Container boots one micro-VM per container.</p>]]></description>
            <enclosure url="https://github.com/bshk-app/ContainerStack/releases/download/v0.2.0/ContainerStack-0.2.0.dmg" length="21221165" type="application/octet-stream" sparkle:edSignature="C1UgwKLoId3tPFZXF/oQz/9RIzeV8IEOqlEhuEtCCVjLFCUQrLbUPV7S2fQUgvMGrAvXUyhxO0CS+W2/8c6jAw=="/>
        </item>
    </channel>
</rss>